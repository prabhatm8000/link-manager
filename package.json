{
  "scripts": {
    "prodBuildClient": "cd server && npm run build && cd ../client && npm run build && cp -r ./dist/* ../server/clientBuild",
    "prodRun": "cd server && npm run start",
    "prodBuildServerOnly": "cd server && npm run build"
  }
}